# Critique-Fix Report — Week 29 (Rebate Economics Decoded)

**Date:** 2026-07-16
**Target:** `newsletters/week_29_rebate_economics.md` (next-to-publish week; W29 Monday = July 20, 2026)
**Command:** `/critique-fix`
**Result:** Ship-clean pending 3 external/human inputs (all already flagged in the file). No fabrication, no strategy overrides.

---

## Summary

W29 is a well-built, previously-critiqued week and came in unusually clean. The full pre-ship pass (brand rules, voice fingerprint, working-framework check, Thursday/Tuesday Excellence, triple-distinct, 6-beat, shocking-fact sourcing, cross-week, Run of Show reconciliation, templated-section completeness) found **no content-quality or brand defects to rewrite**. The only resolvable items were two housekeeping fixes; everything else that remains open is genuinely external (post-publish URLs) or a documented human judgment call.

Per-post verdict:
- **Post 105 (Tue, audit rights, Sticky Note Dashboard):** PASS. Confrontational knowledge-gap hook, "we" proprietary anchor, first comment routes to the Contract Language Library. (One judgment note below re: the Jul 3 reveal-slot cadence.)
- **Post 106 (Wed, offshore rebate aggregator, POV):** PASS. News-pegged named-actor hook (FTC / CVS Caremark / Zinc), reveal shape, proprietary anchor, all facts verified in `shocking_fact_bank.md`.
- **Post 107 (Thu, copay-maximizer reveal, NON-ESSENTIAL Stamp photoreal):** PASS. Thursday Excellence 5/5 already documented in-file; names Prescription Benefit Solutions; J&J v. SaveOnSP magnitude stated qualitatively (no fabricated number).
- **Post 108 (Fri, site-of-care tease):** PASS. Light tease; named-actor ("chosen by the PBM or the provider network") carries it.
- **Newsletter + publish post + first comment + "What I'd Ask" #7 + PART 4B X stream:** PASS. Publish post is a proper micro-reveal (FTC news peg + named entities + convergence + owned stakes).

**Highest-risk item of the week:** the Potter Piece 1 cascade dependency — three callbacks + two X/roundup actions are wired to a HEALTH CARE un-covered URL that must be confirmed live before Monday (see Left-Flagged #2). The file already carries a clean slip-contingency to strip them if it is not.

---

## 1. Auto-fixed (resolvable)

1. **Run of Show — Monday Notes launch-teaser time drift.** The "Note #1 launch teaser" row was timed "Evening" and placed between the 5:00 PM and 7:00 PM X slots, but PART 4 schedules the launch teaser as a **Monday morning (7-8 AM)** note that ships with the Monday article. Retimed to `7-8 AM` and moved up to sit directly after the 7:45 AM newsletter row, restoring true publish order. (`week_29_rebate_economics.md`, Weekly Run of Show table.)

2. **Displaced Field Note parked to the backlog (the file's own pending maintainer action).** The PART 1B parking block for "Auditing Your Pharmacy Network Configuration" (the network-decoder's operational pair, displaced when Thursday was rebuilt into the copay-maximizer reveal) instructed a maintainer to move it into `field_note_backlog.md` and delete the block. Git history confirms this Field Note only ever existed as the framework summary in that block (no fuller body was lost). Actions taken:
   - Added **FN-20 "Auditing Your Pharmacy Network Configuration"** to the `field_note_backlog.md` inventory table (PBM Contract Insights; W29 displacement provenance; `available`).
   - Added the full **FN-20 detail block** (answer-first thesis, three audit passes, pairing to a future Pharmacy-Network / Site-of-Care Thursday + the `network-configuration-audit` toolkit, re-anchoring notes, plus a flag that the body still needs expansion to ~600 words before publishing).
   - Replaced the W29 parking block with a one-line pointer to FN-20 (satisfies "don't let a drafted Field Note disappear" without losing the audit trail).

No toolkit HTML/PDF/PNG was touched (no toolkit minted or edited this week — W29 correctly refers back to the existing Copay Card Financial Impact Calculator per the refer-back-first rule), so no re-render/`_audit_pdfs.py` pass was required.

---

## 2. Left flagged (needs you — external inputs / judgment calls)

1. **Post-publish Substack URLs (external, resolve only after each post publishes).** All already marked ⚠ in the file; the `weekly-substack-urls` workflow auto-fills most from RSS:
   - Newsletter body + Newsletter First Comment: `[SUBSTACK URL]` / `[Substack deep-dive URL...]` (Mon deep dive, publishes 7:30 AM Mon).
   - Post 106 first comment: `[SUBSTACK MONDAY DEEP DIVE URL]`.
   - Post 107 first comment: `[SUBSTACK FIELD NOTE URL]` (Thu Field Note, publishes 7:30 AM Thu).
   - Substack Notes launch teasers: `[LINK]` × 3 (Mon deep dive / Wed roundup / Thu field note).

2. **Potter Piece 1 cascade dependency (human confirmation before scheduling Sunday Jul 19).** The deep dive "Related reading" callout, the newsletter second-paragraph callback, and the publish post news-peg line all reference `https://healthcareuncovered.substack.com/p/the-markup-you-were-never-shown-the` as **published Tue Jul 14**. `CLAUDE.md` (updated Jul 8) still lists Piece 1 as "drafted, awaiting Ginny review + submission," so this is unverified here. **Action:** confirm Piece 1 is actually live at that URL before Monday. If it is NOT live, execute the file's existing SLIP CONTINGENCY (strip the three callbacks; skip the two cascade actions). Do not schedule a reference to a piece that is not live.

3. **Wednesday roundup (Jul 22) — add the Potter piece as a curated story.** Human action already flagged in the Potter Cascade Layer: the Tuesday auto-draft won't include it; add it (with Ginny's plan-sponsor take) when reviewing the draft, and make it the named-entity teaser lead if no bigger story broke.

4. **LinkedIn-first rotation call (judgment — Ginny's).** The Mon 8:00 AM X "audit rights limitation" recipe shares a broad topic with Tue's Post 105 and posts ~a day earlier. It's a generic evergreen recipe (distinct angle from Post 105's unused-window finding), so it's a soft overlap, not a reveal scoop. Reorder after Post 105 publishes or leave as-is — already flagged in-file.

5. **Reveal-slot cadence vs. as-built Tuesday (strategy note, NOT auto-changed).** W29 was built June 2026, before the **Jul 3 reveal-slot cadence** rule that makes non-Library Tuesdays a standing reveal slot. Post 105 is a Sticky Note Dashboard decoder ("Tuesday Cycle-W1," the superseded 6-week pillar-cycle logic). Left as-is deliberately: W29 already runs **two** reveals (Wed offshore aggregator + Thu copay maximizer), and the cadence rule itself warns against reveal fatigue/wallpaper, so the audit-rights decoder is defensible as format variety. Flagging for Ginny's awareness only — reshaping a whole post's format is a judgment call, not an auto-fix.

---

## 3. Ship-readiness

**Ship-clean except for the external inputs above.** No brand-rule, voice, framework, fact-sourcing, or structural defect remains. Confirmed clean on this pass:
- **Brand:** no em-dash / " - " sentence separators in any published copy (all em-dashes/hyphens are in internal build notes, visual specs, or format-label headers); no bare "PBS" in public copy (public surfaces spell out "Prescription Benefit Solutions" / use "we"; the only bare "PBS" instances are internal logo/build instructions); no "RXBS"; no "Dr. Ginny Crisp." Hashtags 3-per-post, PascalCase, pillar-correct. All paste-ready blocks fenced; X bodies ASCII.
- **Facts verified in `shocking_fact_bank.md`:** FTC–Caremark settlement + $8.5B / Express Scripts Feb / OptumRx consent (VERIFIED PRIMARY SOURCE, entry line 120, FTC press-release URL matches); offshore rebate aggregators Ascent (Switzerland) / Emisar (Ireland) / Zinc + $7.6B 2022 fees (entry line 44, Frier Levitt source); copay maximizer / J&J v. SaveOnSP (#5). No fabricated statistics; litigation magnitude stated qualitatively per the no-fabricated-numbers rule. All "last week" temporal references check out against the July 20 publish date.
- **Structure:** Run of Show reconciled to built content and consistent with neighbor weeks (no roundup row, matching W28/W30 convention); newsletter Issue #24 correct in sequence (W27 #22 / W28 #23 / W30 #25); all mandatory templated sections present (Title Options, Publish Post Copy, First Comment, PART 1C, PART 2B, PART 4B). No Library-week section needed (W29 is not a Library week). All six referenced toolkit slugs (`copay-card-calculator`, `channel-pricing`, `pbm-compensation`, `step-therapy-override`, `pa-roi-audit`, `network-configuration-audit`) resolve to real dataset rows.
- **Craft:** triple-distinct holds (Mon rebate flow / Tue audit rights / Thu copay maximizer); Thursday Excellence 5/5; no AI-tell blacklist phrases, exclamation marks, or banned intensifiers in published copy.

# Critique-Fix Report — Week 28 (Pharmacy Network Design)

**File:** `newsletters/week_28_network_design.md`
**Run date:** 2026-07-09
**Publishes:** July 13-17, 2026 (next-to-publish; W27 is the current live week)
**Command:** `/critique-fix` (diagnose → apply resolvable fixes → flag the rest)

---

## Per-post scorecard (Phase 1 diagnose)

| Post | Day | Result | Note |
|------|-----|--------|------|
| Deep dive (Network Design, 5-channel model) | Mon | PASS | Answer-first lead, question-shaped AEO title, outbound rxbs.org canonical links, firm name spelled out. Integrated tool = live Channel Pricing Worksheet (reused per refer-back-first rule). |
| LI Newsletter (Issue #23) | Mon | **FLAG → FIXED** | Issue # correct (W27=#22 → W28=#23). First-comment toolkit was off-topic + unbuilt; fixed (see Auto-fixed #1). |
| Post 101 — GLP-1 "Bridge" gauntlet (messy infographic) | Tue | PASS | Confrontational named-actor hook (denial required *first*), proprietary anchor ("I read prior-auth criteria for a living"), plan-sponsor-actionable, first comment → Contract Language Library. |
| Post 102 — SHRM clip4 "1 in 3 would leave" (9:16 video) | Wed | PASS | Stat-shock retention hook; video swaps the Wed POV per the video-tab rule; link in first comment → drug-pipeline toolkit (on-topic). |
| Post 103 — "12-cent generic / $9" generic-spread REVEAL | Thu | PASS (strongest of week) | All 5 Thursday Excellence traits hit; sourced FTC Jan-2025 staff-report fact woven in with attribution; proprietary anchor; first comment names the same-day Field Note + GER worksheet by asset. |
| Post 104 — "money trail your claims report doesn't show" tease | Fri | PASS | Confrontational hook; correctly teases W29 (rebate economics). |
| PART 4B — X (39 posts) | all | PASS | Bodies are clean ASCII; links in first reply only; 0-1 hashtags; LinkedIn-first respected (long-form riffs Monday's already-public topic, doesn't scoop Thursday). |

**Cross-week / triple-distinct:** Mon Network Design / Tue GLP-1 Bridge access / Thu Generic spread (GER) — three distinct subjects. Holds.

---

## 1. Auto-fixed (resolvable, applied in place)

1. **Newsletter first comment routed to the wrong toolkit** (PART 2, "LinkedIn Newsletter First Comment"). The third line advertised the `ger-audit` (Generic Effective Rate) worksheet — off-topic for a network-design briefing, *and* gated behind a Wix page not built until this week's Thursday toolkit ships. Per CLAUDE.md convention (c), the newsletter first comment routes to the **Monday deep dive's own companion tool**, which here is the **Channel Pricing Audit Worksheet** (`rxbs.org/toolkit/channel-pricing`) — on-topic (retail/mail/specialty net-cost comparison) and already live (W18). Rewrote the line and its publisher note accordingly; the GER worksheet is cross-promoted on the Thursday posts where it belongs.

2. **Run of Show Mon 7:45 AM row referenced the same wrong toolkit.** Line said "confirm ger-audit toolkit page renders before posting first comment." Updated to note the first-comment toolkit is the already-live `channel-pricing` worksheet (removes a false pre-ship blocker; the only real ⚠ on that row is the live deep-dive URL paste).

_Re-scanned after edits: no new em-dash separators, no bare "PBS" in public copy, no non-ASCII introduced._

---

## 2. Left flagged (needs you — genuinely external / human-supplied)

These are all not-yet-existing URLs or human data. Per the command's guardrails, not invented.

1. **Newsletter body `[SUBSTACK URL]`** (PART 2, "This week's Benefit Blind Spots includes a network cost comparison model … [SUBSTACK URL]") — paste the live deep-dive URL after Mon 7:30 AM publish.
2. **Newsletter first comment `[Substack deep-dive URL …]`** — same live deep-dive URL, paste after Mon 7:30 AM.
3. **Post 103 first comment Field Note URL** (`[SUBSTACK FIELD NOTE URL — slug generic-spread-ger-average-hides]`) — paste after the Thu 7:30 AM Field Note publishes. Already ⚠-flagged in the Run of Show.
4. **PART 1C `pdf_url` for ger-audit** — awaits Wix Media upload of `week_28_thursday_ger_audit_worksheet.pdf` (file exists in `templates/documents/`).
5. **PART 1C `field_note_url` for ger-audit** — awaits the Thu Field Note publishing (same slug as #3).
6. **Substack Notes `[LINK]` placeholders** (Mon article / Wed roundup / Thu field-note launch teasers) — paste each live URL after the corresponding post publishes.
7. **PART 7 metrics tables** (LinkedIn feed metrics, video test #2) — human-supplied post-publish performance data.

---

## 3. Minor observations (not fixed — judgment calls, left to the author)

- **Tue (GLP-1 Bridge access) and Wed (GLP-1 retention) both sit in GLP-1 territory.** Different subjects (utilization-management gauntlet vs. retention business case) and the triple-distinct rule governs Mon/Tue/Thu+Field Note (which are distinct), so this is not a violation — but two GLP-1-flavored days back-to-back is worth an eye. No change made.
- **Tue X 8:00 AM + 2:30 PM first replies route to `rxbs.org/toolkit/network-configuration-audit`** (a W29 toolkit; files exist but the Wix landing page may not render until W29 ships next week). This is an established `x_recipe_post_bank` mapping used week-to-week; left as-is rather than re-routing (a routing swap would be a judgment call). Confirm the page renders before those replies post, or swap to the Contract Language Library.

---

## Ship-readiness

**Ship-clean except for the 7 flagged external inputs above** — every one is a live-URL paste (deep dive, field note, notes teasers, Wix `pdf_url`) or human performance data. No strategy-level reshape needed; the week's content, structure, brand rules, and mandatory sections all pass. The Thursday generic-spread reveal is the week's strongest post and is fully built (copy + FTC-sourced fact + Field Note + GER worksheet).

**Note:** The ger-audit toolkit triplet (HTML/PDF/PNG) already exists in `templates/documents/`; no toolkit HTML was edited this pass, so no re-render/audit was required (`_audit_pdfs.py` needs `pymupdf`, absent in this environment — not run since nothing was re-rendered).

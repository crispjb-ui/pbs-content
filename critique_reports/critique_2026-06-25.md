# Critique-Fix Report — Week 26 (Formulary Management)

**Target:** `newsletters/week_26_formulary_management.md`
**Publish window:** Mon Jun 29 – Fri Jul 3, 2026 (July 4 week)
**Run date:** 2026-06-25 (Thursday). Next-to-publish week = W26 (today is past W25's Monday Jun 22, so current_week+1 = 26).
**Mode:** `/critique-fix` — diagnose, then apply every RESOLVABLE fix in place; leave only genuinely-unresolvable items flagged. **Not committed** (the next step commits the week file + supporting files + this report together).

---

## 1. Auto-fixed (resolvable — applied in place)

### Posting-time drift → 8:30 AM (rule-of-record conformance)
The file carried the concluded 10:00 AM posting-time test framing. CLAUDE.md is explicit: the 10:00 AM test concluded W18 and **reverted to 8:30 AM starting W19**; the adjacent already-corrected week (W25) uses 8:30 AM. W26 (and W27) still showed 10:00 AM — stale leftover. Normalized all LI Feed times to 8:30 AM:
- Run of Show rows: Tue Post 93, Thu Post 95, Fri Post 96 → 8:30 AM (Wed Post 94 was already 8:30).
- Post headers: Post 93 / 95 / 96 "Tuesday/Thursday/Friday 10:00 AM" → "8:30 AM"; parked Wednesday alternative "Post Time" → 8:30 AM.
- Top-of-file NOTE: removed "Testing 10:00 AM post time" → "Feed posts publish at 8:30 AM EST (standard since W19; the 10:00 AM test concluded W18 and reverted)."
- PART 4B LinkedIn-first note: "(listed 10:00 AM; may revert to 8:30)" → "publishes Tue/Thu 8:30 AM."
- PART 5 Content Schedule: all four feed posts → 8:30 AM; "Track 10:00 AM posting time performance" → "Feed posts publish at 8:30 AM (10:00 AM test concluded W18, reverted W19)."
- PART 7 metrics: feed-table times → 8:30 AM; replaced the now-moot "Posting Time Test Results" table with a "Posting Time" note recording that the test concluded W18.
- (X weekend 10:00 AM Tier-A slots left intact — that is the X weekend cadence, not the feed-time test.)

### PART 1C dataset — wrong field-note pairing (copy-paste error from a prior week)
- `field_note_title`: `What We See When We Audit Channel Pricing` → **`Auditing Your Step Therapy Override Workflow`** (this week's actual Field Note).
- `field_note_url`: `…/p/one-drug-class-to-watch-the-next` → **`…/p/audit-step-therapy-override-workflow`** (derived from the Field Note's own SEO slug, used consistently elsewhere in the file), with an inline "(live once the Field Note publishes Thu Jul 2)" note.
- Pairing rationale: rewrote the stale "Channel Pricing Field Note carries the same audit posture" → correctly references the PBM Compensation "zoom out" pairing and this week's Step Therapy Override Field Note.

### Stale cross-references reconciled to as-built content
- **In-Article Visual pointer (Post 95 lead-in):** "Formulary Evaluation Framework contract-markup carousel … In-Article Visual: Formulary Evaluation Framework" → **"Formulary Independence Scorecard"** (the visual that actually lives in PART 1).
- **PART 4B LinkedIn-first note:** named two LinkedIn posts that don't ship this week ("The Formulary Decision You Never Made" Marked-Up Contract Page; "5 Rebate Retention Terms Decoded" messy infographic) → rewritten to the as-built Tue Post 93 (Bypass/Funnel) and Thu Post 95 (Step Therapy Override Sticky Note).
- **Orphaned Tue 5:00 PM X "decoder driver":** body was a formulary-control decoder driving a Tue LinkedIn post that was swapped out. Re-angled to amplify the actual Tue Post 93 (five manufacturer payments that bypass the rebate pass-through); first reply (Contract Language Library) already matched and was kept.
- **Thu 12:00 PM X amplifier:** `[VISUAL: 5 Rebate Retention Terms Decoded …]` + rebate-retention body → relabeled to `[VISUAL: 5 Step Therapy Override Terms Decoded Sticky Note Dashboard messy infographic]` and rewritten to the step-therapy-override concept (first reply already pointed to the correct Field Note URL).
- **Thursday Substack Notes Field Note teaser:** described the displaced "three formulary questions" draft → rewritten to "Auditing Your Step Therapy Override Workflow" (URL was already correct).
- **Wed 7:00 PM X [A8] first reply:** falsely claimed "this week's deep dive walks the H1 review" (this week's deep dive is Formulary) → "The mid-year renewal-readiness tools live on Benefit Blind Spots" (truthful, evergreen route).
- **PART 5 verify line:** "Verify Tuesday messy infographic … (different angle: PBM decision flow)" → corrected to the as-built "Bypass/Funnel clean infographic … manufacturer payments that bypass the rebate pass-through."

### Brand-rule conformance (public-copy entity naming)
Spelled out "Prescription Benefit Solutions" / used first-person "we" for bare "PBS" in **public surfaces** (kept "PBS" only in internal build/production notes per the rule):
- Post 93 copy: "…a year at PBS." → "…a year at Prescription Benefit Solutions."
- Post 94 (video) copy: "We review hundreds of PBM contracts a year at PBS." → "Spread language sits in nearly every PBM contract we read at Prescription Benefit Solutions." (also varies the Positioning anchor — see below.)
- Post 95 copy: "…on plan claims at PBS…" → "…at Prescription Benefit Solutions…"
- Post 95 image-rendered sticky-note text: "PBS reviews 100s of PBM contracts a year." → "We review 100s of PBM contracts a year." (text renders onto a public image.)
- Field Note Meta Description: "the pattern PBS sees most often" → "the pattern we see most often."

### 6-beat Positioning-anchor variation
Posts 93 and 94 both opened their Positioning beat with the identical "We review hundreds of PBM contracts a year at PBS" (LinkedIn flags repeated boilerplate footers). Varied Post 94's anchor to the spread-language phrasing above so the two consecutive posts no longer share a verbatim anchor (Post 95's "we audit how step therapy actually runs on plan claims" was already distinct). 2025 firm metrics intentionally **not** injected — `proprietary_anchor_bank.md` holds them for Q3 (Sep–Oct surge); W26 keeps qualitative anchors.

### Run of Show gap
- Added the missing **Wed 12:30 PM long-form X** row (the long-form-X trial post exists in PART 4B but was absent from the Run of Show).

### Hashtag rule
- Friday tease (Post 96) had **zero** hashtags → added pillar-appropriate `#PBMContracts #PBMRFP #PharmacyBenefits` (PBM Contract Insights / next-week RFP anchor; varies 2 of 3 tags vs. Post 95 to avoid a repeated block).

### Minor factual fix (parked content)
- Parked Wednesday-alternative first comment said "the 4 questions to ask your PBM" → "the seven questions" (the deep dive has seven), so the count is correct if the parked draft is ever re-slotted.

---

## 2. Left flagged (needs you — unresolvable without external input)

1. **Toolkit `pdf_url`** (PART 1C) — still `[fill after Wix Media upload]`. The toolkit itself is built and clean: `templates/documents/week_26_thursday_step_therapy_override_audit.{html,pdf,png}` exist and the PDF **passes `_audit_pdfs.py` (2pp, 0 flagged)**. Only the hosted URL is external: upload the PDF to Wix Media and paste the public URL into the `pdf_url` cell.
2. **`rxbs.org/toolkit/step-therapy-override` landing page** — newsletter first comment, Field Note body, and the toolkit routing all point here. Build/verify the Wix dynamic page renders before those links go live (the file's own publisher note already flags this; if not built by Monday, drop the toolkit line and route to the deep dive).
3. **`field_note_url`** — filled with the derived slug `…/p/audit-step-therapy-override-workflow`, but the Substack page only resolves once the Field Note publishes **Thu Jul 2 7:30 AM**. Confirm it's live before the Wix row goes live (per the PART 1C checklist).
4. **Wednesday Notes roundup teaser `[LINK]`** (PART 4 · Wed morning) — the "What Crossed My Desk" roundup URL isn't known until that roundup is drafted/published **Wed Jul 1**. Paste the live Substack roundup URL.
5. **Post 94 native 9:16 video render** — `social_clips/remotion_starter/out/2026-06-09_spread-pricing-glp1_9x16.mp4` is **not present in the repo** (the render is produced by the external Remotion/editing project). Confirm the 9:16 file is rendered before Wed Jul 1, or swap to the PARKED Wednesday POV-text alternative already preserved in PART 3.

_Not defects (left intentionally):_ the A/B first-comment-timing variant assignments (deliberate experiment) and the empty PART 7 metrics tables (post-publish human data).

---

## 3. Ship-readiness

**Ship-clean except for 5 flagged external inputs**, all of which are the expected publishing-day hand-offs (a Wix Media upload + landing-page render, a not-yet-published Field Note URL, a not-yet-published roundup URL, and the externally-rendered video file). No strategy-level reshape is needed: the triple-distinct topic split holds (Mon Formulary / Tue Manufacturer payments / Thu Step Therapy Override), the Thursday Excellence stack is intact (named-actor hook + proprietary anchor + first-comment names the same-day Field Note + Field Note exclusively supports the concept), the AEO gate passes (question-shaped SEO titles, answer-first leads, outbound rxbs.org links on both Substack pieces), and the toolkit passes the bleed audit.

**Note for the next build:** W27 (`week_27_pbm_evaluation.md`) carries the same stale 10:00 AM posting-time framing across its Run of Show, post headers, and checklist; apply the same 8:30 AM normalization when it comes up for ship.

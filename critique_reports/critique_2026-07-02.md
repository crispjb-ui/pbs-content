# Critique-Fix Report — Week 27 (PBM Market: Stay or Switch)

**Run date:** 2026-07-02
**Target:** `newsletters/week_27_pbm_evaluation.md` (publish week Mon Jul 6 – Fri Jul 10, 2026)
**Command:** `/critique-fix` (diagnose → apply resolvable → flag the rest; no commit — next step commits)

**Week selection:** anchor W25 Mon = 2026-06-22 → `25 + floor((2026-07-02 − 2026-06-22)/7) = 25 + floor(10/7) = 26`. Today (Jul 2) is past W26's Monday (Jun 29), so per the rule the next-to-publish week is **current_week + 1 = 27**.

---

## 1. Auto-fixed (resolvable — applied in place)

| # | Finding | Location | Fix applied |
|---|---------|----------|-------------|
| 1 | **Stale ⚠ "open item": deep-dive aggregator-parent mapping claimed inverted.** The file's own highest-risk flag (Run of Show intro + Post 99 note) said the deep-dive body inverted Ascent/Emisar/Zinc and had to be corrected before Monday. **Verified false:** the deep-dive body (Factor 2) and the Field Note already carry the CORRECT sourced mapping (Ascent under Cigna/Express Scripts, Emisar under UnitedHealth/OptumRx, Zinc under CVS Caremark). The warnings were stale. | Run of Show intro (~L8), Post 99 note (~L788) | Rewrote both to confirm the mapping is verified consistent/correct across the deep dive, Field Note, and Post 99. Removed the ⚠ open item. (No content edit to the body was needed — it was already right; also cleared an em-dash in the old warning.) |
| 2 | Em-dash used as a sentence separator in the Substack **Subtitle** (published prose — brand rule). | Deep-dive Subtitle (~L78) | `layered on top —` → `layered on top:` |
| 3 | Duplicate/stale **Subtitle** line (superseded pre-reframe original: "A 100-point weighted scorecard that removes the guesswork…"). Substack has one subtitle field; two was confusing stale content. | Deep-dive meta (~L82) | Removed the superseded duplicate; kept the reframed Subtitle + the separate SEO Subtitle. |
| 4 | Em-dash sentence separator in the **Meta Description** (published SEO copy). | Article Meta (~L297) | `layered on top — five factors` → `layered on top. Five factors` |
| 5 | **Bare "PBS" in public LinkedIn copy** (naming rule: spell out "Prescription Benefit Solutions" in published surfaces). | Post 97 carousel Post Copy (~L658) | `hundreds of these contracts a year at PBS` → `…at Prescription Benefit Solutions` |
| 6 | **Post 97 Company Reshare clause-list drift** — reshare named a different five clauses (frequency / scope / data format / third-party access / audit window) than the carousel actually ships. | Post 97 Reshare (~L710) | Rewrote to the actual five: data delivery, guarantee methodology, benchmarking rights, specialty-routing disclosure, findings remediation. |
| 7 | **Post 98 (Wed POV) missing hashtags** (3-per-post rule + §8 gate; week_26 Wed/Fri text posts confirm the convention). | Post 98 copy | Added `#PBMContracts #PBMRenewal #SelfFundedEmployers` (PBM Contract Insights pillar + renewal topic anchor; differs from the consecutive prior feed post's set). |
| 8 | **Post 100 (Fri tease) missing hashtags.** | Post 100 copy | Added `#PharmacyCosts #NetworkDesign #SelfFundedEmployers` (Cost Containment pillar + network-design topic anchor for the W28 tease). |
| 9 | **Monday deep-dive URL placeholders** (`[SUBSTACK URL]`, `[Substack deep-dive URL, paste after…]`, `[SUBSTACK MONDAY DEEP DIVE URL]`, Monday launch-Note `[LINK]`). The canonical URL is already committed in-file (X long-form first reply) and the deep dive publishes Mon 7:30 AM — before every reference. Reconciled internal inconsistency, not invented. | Newsletter body CTA (~L483), Newsletter first comment (~L524), Post 98 first comment (~L775), Monday Note (~L928) | Filled all four with `https://benefitblindspots.substack.com/p/pbm-vertical-integration-stay-or-switch-2026`. |

**Post-fix re-scan (Phase 3):** no bare "PBS" left in public copy; no em-dash separators in any published prose (remaining em-dashes are all internal build notes / A/B annotations / the image prompt / title-option notes — allowed); all PART 4B X post bodies remain pure ASCII (non-ASCII chars are only in labels/first-reply instruction lines, none inside fenced blocks).

---

## 2. Left flagged (needs Ginny — genuinely external / not derivable in-repo)

| # | Item | Location | What's needed |
|---|------|----------|---------------|
| 1 | **Field Note URL** (publishes Thu Jul 9; its full URL is not committed anywhere in-file — only the locked slug `pbm-rebate-aggregator-audit`). Not constructed, to avoid fabricating a URL Substack may suffix. | PART 1C `field_note_url` dataset cell (~L434) **and** Post 99 Thursday first comment `[SUBSTACK FIELD NOTE URL]` (~L839) | Paste the live Field Note URL after Thu 7:30 AM publish (likely `…/p/pbm-rebate-aggregator-audit` — verify). The PART 1C convention also gates the Wix row from going live until this is a published URL. |
| 2 | **Wed roundup Note `[LINK]`** — the Wednesday "What Crossed My Desk" roundup is a separately-generated file; its URL isn't in this week file and it publishes Wed Jul 8. | Substack Notes → Wednesday teaser (~L954) | Paste the roundup URL after it publishes Wed. |
| 3 | **Thu field-note Note `[LINK]`** — same asset as flag #1, in the Thursday Substack Notes teaser. | Substack Notes → Thursday teaser (~L967) | Paste the live Field Note URL after Thu publish. |
| 4 | **Wix toolkit live-ops** (human steps, not file edits): upload `week_27_thursday_pbm_compensation_audit` PDF+PNG, add the `pbm-compensation` row, and confirm `rxbs.org/toolkit/pbm-compensation` renders before the Thursday first comments route to it. `pdf_url` is already populated in PART 1C. | PART 1C checklist | Run the checklist; if the landing page isn't live by Thu, drop the toolkit line and route to the deep dive (per the existing PART 2 note). |

**Deliberately not touched (judgment calls, per guardrails):**
- **A/B first-comment-timing variants** (Variant A control vs Variant B treatment across Posts 97/98/99 + newsletter) — deliberate experiment design.
- **Newsletter Title Options** (3 offered) — author's pick.
- Minor observation only (no change): the In-Article Visual scorecard carries the "Illustrative example… Actual amounts vary by plan." disclaimer, but the visual shows point scores, not dollar figures/percentages, so per the disclaimer policy it is arguably unnecessary. Left as-is (inside a build spec, low-risk, defensible for a scoring rubric).

---

## 3. Ship-readiness

**Ship-clean except for 4 external inputs** (all in Section 2): the Field Note URL (2 spots + the Thursday Note teaser), the Wednesday roundup URL (Note teaser), and the Wix toolkit landing-page live-check. No strategy-level reshape is required.

Gate summary (verified against `week_build_spec.md` §8):
- **Highest-risk flagged item resolved** — the aggregator-parent mapping is correct and consistent across the deep dive, Field Note, and Post 99; the stale ⚠ open item is cleared.
- **Triple-distinct holds:** Mon = PBM evaluation (stay/switch) · Tue = reporting clauses (Library 05) · Thu = offshore rebate aggregators. Three distinct subjects.
- **Summer reach gate (§2):** the Thursday reach slot IS a shocking hidden-structure reveal (offshore rebate aggregator), not a vocabulary decoder — correct for the summer trough.
- **Shocking fact woven + cited:** $7.6B FTC aggregator-fee figure + May 2026 CVS 340B lawsuits + Pennsylvania Act 77 study, paired with the PBS proprietary anchor (affiliated-entity audit-rights clause in fewer than half of contracts reviewed).
- **Brand rules:** no em-dash separators in published prose; no bare "PBS" in public copy; Ginny Crisp, PharmD; 3 PascalCase hashtags per post; X bodies pure ASCII; paste-ready blocks fenced. All pass.
- **Newsletter issue #22** verified correct (W25 = #20, W26 = #21).

# Case Studies (internal until permissioned)

_Win stories captured via `engagement_closeout_kit.md`. Naming: `YYYY_MM_{slug}.md`. Publish NOTHING from here without the permission status field saying so._

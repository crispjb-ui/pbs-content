# Pre-Ship Critique — Week 24 (H1 Review / H2 Preview)

**File critiqued:** `newsletters/week_24_h1_h2_review.md`
**Publish window:** June 15–21, 2026 (Q2 close)
**Critique date:** 2026-06-14
**Reviewer:** /critique (advisory only — no week-file edits made)

**Week determination:** W25 Mon = 2026-06-22 → current_week = 25 + floor((06-14 − 06-22)/7) = 23. Today (06-14) is past W23's Monday (06-08), so per the rule the next-to-publish week is **W24** (Monday 06-15).

---

## Verdict at a glance

The core conversion stack is strong: the Monday deep dive, the LinkedIn newsletter, and the Thursday Excellence triad (whiteboard timeline + amendment Field Note + Contract Amendment Letter toolkit) are well-built and on-profile. **But the week is NOT ship-ready as written.** Four concrete defects need fixing before scheduling, two of them structural:

1. **Tuesday carousel is mislabeled "Library 03" — that number already shipped (W23, Audit Rights, Jun 9).** Hard collision. Must renumber.
2. **W24 ships a Library entry but is missing the required `## Substack Contract Library Update — Library 0X Week` section.**
3. **PART 1C Wix rows point both toolkits at the wrong Field Note** (Channel Pricing / a W18 URL), not this week's amendment Field Note.
4. **Thursday post copy uses markdown `**bold**` inside a paste-ready code block** — LinkedIn renders the asterisks literally.

Plus a reach risk: the week clusters topically — two "numbers/reporting" posts (Mon, Wed) and two "contract-clause" posts (Tue, Thu) — and **Wednesday reuses Monday's exact hook verbatim.**

---

## Per-post scoring

### PART 1 — Monday Substack Deep Dive: "H1 Review: What the First Half Data Shows" — **PASS (with one soft flag)**
- **Hook/working framework:** Strong. "Your PBM has already run your H1 numbers… I would bet money you have not looked at them yet." Knowledge-gap + named adversary (PBM holding the information advantage). Decoder/framework shape, in the WORKING set.
- **Proprietary anchor:** Present and unfakeable — "I have watched that single number shift an entire negotiation," "We see this disconnect frequently. It is not always obvious from summary reports." ✓
- **Structure:** Story-driven open, thesis early, 6 bold section headers, integrated tool (5-metric scorecard) as blockquotes (not tables) per the Substack rule. Disclaimer correctly placed *after* the quantitative metrics, not after narrative. ✓
- **SOFT FLAG — possible invented specificity in the supporting carousel/notes ecosystem, not the article itself:** the article keeps its benchmark ranges general and disclaimed (GER 85%, specialty 40–55%), which is fine. But watch the "12–18 percent of H1 spend" figure that appears on the Tuesday carousel slide 2 (see below) — that one reads as a hard fabricated stat. The deep dive is clean; flagging here only so the number doesn't get treated as house-validated.
- **Fix:** None required for the article. Ship.

### Monday In-Article Visual ("Your H1 Dashboard" clean infographic) — **PASS**
- Correctly relocated into PART 1 as an `### In-Article Visual` per the deep-dive-visual rule (not left as a dead Thursday block). Flat-design, mobile-legible (5 rows, large type), no logo in the generation prompt. ✓

### PART 2 — LinkedIn Newsletter (Issue #19) — **PASS**
- Confrontational executive open, specific Substack-exclusive CTA ("H1 benchmark dashboard… renewal readiness scorecard"). ✓
- All three required subsections present and correct: **Title Options** (3, spanning the prescribed shapes), **Publish Post Copy** (~65 words, 3 PascalCase hashtags, fenced), **First Comment** (topic line + deep-dive line + toolkit line `rxbs.org/toolkit/h1-renewal-readiness`, fenced, with the publisher note about the link going live). ✓
- Minor: the newsletter body and the Monday deep dive open on the same sentence. That is the intended bridge (acceptable) — but it makes the **Wednesday** reuse of the same hook the third occurrence (see Post 86).

### PART 3 / Post 85 — Tuesday Library Carousel: "Three Clauses Behind the Cost Patterns That Surface at Mid-Year" — **FLAG (structural)**
- **Framework:** Library NN reference asset — WORKING set, correctly graded on saves not the 20–25K floor. Slide spec matches Library 01/02 standard (off-white paper, red strikethroughs, Accent-Blue insertions, "WHY IT MATTERS" panels). Craft is good.
- **FLAG 1 — duplicate Library number (must fix before ship).** This carousel is labeled **"Library 03"** throughout (Run-of-Show, header, slide eyebrow "PBM CONTRACT LANGUAGE · LIBRARY 03", Assets table). But **Library 03 already shipped in W23 on Jun 9, 2026 as "Five clauses in your PBM audit rights provision" (Audit Rights).** Confirmed in `week_23_mid_year_claims.md` (Post 81, slide eyebrow LIBRARY 03) and in `linkedin_performance_tracker.md` (Tue Jun 9 Library 03 entry). Shipping a second "Library 03" breaks the reference-series integrity the whole Library play depends on.
  - **Fix:** Renumber this carousel to **Library 04** everywhere it appears (cover eyebrow, post header, Run-of-Show, Assets-Needed table, first comment if it cites a number). Note this also collides with CLAUDE.md's planned "Library 04 — W25"; reconcile the forward schedule (either W25 becomes 05, or confirm W25 is not a Library week per the "realigned to as-built" note) so two future entries don't both claim 04.
- **FLAG 2 — missing required section.** CLAUDE.md mandates that **every Library-shipping week file carry a `## Substack Contract Library Update — Library 0X Week` section** (provisions added/amended + the paste prompt). W23 has it; **W24 does not.** This week surfaces three new clauses (§5.04 specialty routing, §3.07 new-molecule placement, §4.08 channel pricing) that should be reflected in `substack_contract_language_library.md` and pushed.
  - **Fix:** Add the section (after PART 2B's "What I'd Ask"/before the next non-Substack section), listing the three provisions, and confirm they're merged into the Library article body before the carousel ships.
- **FLAG 3 — hard stat on slide 2.** "drives 12-18 percent of H1 pharmacy spend" and "In half of the mid-year reviews we run" read as invented precision. The no-fabricated-statistics rule prefers "a disproportionate share" / "in many of the reviews we run" unless 12–18% is a defensible PBS-observed range.
  - **Fix:** Soften to a qualitative anchor ("a single drug can drive a double-digit share of H1 spend") or confirm the range is real and keep it.
- **First comment / reshare:** Both fine (route to Contract Language Library; reshare 48 words, 3 hashtags). ✓ once the number is corrected.

### Post 86 — Wednesday SHRM "Honest HR" clip7 video: "Demand These 3 Numbers" — **FLAG (cannibalization)**
- **Hook:** "Your PBM already knows your H1 numbers. You probably don't." Passes the Wednesday confrontation test (knowledge gap). Proprietary anchor present ("We review hundreds of PBM contracts a year at PBS"). Video off the Thursday breakout day is a reasonable documented format test.
- **FLAG — hook is a verbatim repeat of Monday.** The Monday deep dive ("Your PBM has already run your H1 numbers… You probably have not looked at any of it yet"), the Monday newsletter (same), and this Wednesday video all open on the identical PBM-knows/you-don't construction. Three times in one week is fatigue, and Wednesday is supposed to be a *distinct* topic from Monday. The Wednesday subject (demand 3 reporting numbers: drug cost / rebates / net cost) also sits very close to Monday's "5 numbers." Under the triple-distinct rule this is the borderline pairing of the week — adjacency is the ceiling and Mon↔Wed here are flirting with the same subject (reporting numbers).
  - **Fix:** Reopen the Wednesday video copy on a different angle that doesn't reuse Monday's sentence — lead on the *reporting-access right* / contract clause ("Your PBM owns the report that grades your PBM") rather than "they know your numbers, you don't." Keep the 3-numbers payload; change the opener so Wednesday reads as the reporting-access fight, not a second helping of Monday's H1 read.
- **First comment:** Routes to the Quarterly Reporting Checklist toolkit. ✓

### PART 1B — Thursday Field Note: "Auditing Your Mid-Year Contract Amendment Window" — **PASS**
- Exclusively supports the Thursday LinkedIn post's concept (the 5 decoded amendment clauses → 3 operational passes). Tactical "we see this in our work" voice, proprietary anchor ("across hundreds of PBM contract reviews and audits a year"). Notice-deadline math (90-day notice → Oct 1 for Jan 1 effective) is internally consistent with the post and the toolkit. ✓
- Meta note: SEO/meta description says "100 contract reviews per year" while the body says "hundreds" — harmonize to one figure ("hundreds") for consistency. Minor.

### Post 87 — Thursday whiteboard vertical timeline: "The Mid-Year Amendment Window" — **FLAG (paste formatting) — otherwise the week's strongest post**
- **Thursday Excellence 5-trait check:**
  1. Format = messy-infographic / photoreal whiteboard vertical timeline (active rotation; correctly NOT the paused Whiteboard *carousel*). Mobile-legibility addressed (4:5-native, conference-room marker type, grids eliminated). ✓
  2. Confrontational hook + named actor ("your PBM won't remind you" in-scene; "Most plan sponsors never invoke any of them"). ✓
  3. Proprietary anchor ("I review hundreds of PBM contracts a year at PBS"). ✓
  4. First comment names the same-day Field Note asset specifically (three steps + paste-ready Contract Amendment Letter Template). ✓
  5. Field Note exclusively supports it. ✓
  Date-math gate satisfied: Oct 1 working anchor back-calculated from a Jan 1 effective date under a 90-day clause (literal Oct 3 Saturday → Oct 1 round/weekday-safe). ✓ This is the canonical Thursday build.
- **FLAG — markdown bold inside a paste-ready code block.** The Post Copy fenced block contains `1. **Amendment Trigger Language.**` … `5. **Dispute Resolution Clause.**`. LinkedIn does not render markdown; the asterisks paste literally (`**Amendment Trigger Language.**`). CLAUDE.md is explicit: skip bold or use sparing UPPERCASE.
  - **Fix:** Remove the `**` from all five list items (e.g., `1. Amendment Trigger Language.` or `1. AMENDMENT TRIGGER LANGUAGE —`). This is the one defect on an otherwise ship-ready post.
- **Reshare:** 61 words, accurate Oct 1 timeline, 3 hashtags. ✓

### Post 88 — Friday Tease Hook: "What Q3 Will Bring" — **PASS (two cosmetics)**
- Tease names the PBM's prepared renewal team and sets up the Q3 arc / next-Monday Renewal Countdown. Forward-look is appropriate for the Friday slot. ✓
- **Cosmetic 1:** Header says "**Word Count:** 72 words" but the copy runs ~150 words. Correct the label.
- **Cosmetic 2:** No hashtags (CLAUDE.md baseline is 3/post). Friday teases historically run bare, so this is optional — add a 3-tag Transparency/Self-Funded set if you want the classification signal, otherwise leave intentionally.

### PART 1C — Wix Toolkit Pairings + Build — **FLAG (stale data)**
- Both toolkits are correctly built and present on disk (HTML + PDF + preview PNG + X crop for `week_24_h1_renewal_readiness_scorecard` and `week_24_thursday_contract_amendment_letter`). Slugs (`h1-renewal-readiness`, `contract-amendment-letter`), mechanic phrases, and second-toolkit pairings are coherent. ✓
- **FLAG — wrong Field Note wired to both rows.** Both dataset rows set `field_note_title` = "What We See When We Audit Channel Pricing" and `field_note_url` = `…/p/one-drug-class-to-watch-the-next` (a Channel Pricing / W18-era asset). This week's Field Note is **"Auditing Your Mid-Year Contract Amendment Window."** Email 1/Field-Note merge will deliver the wrong link.
  - **Fix:** Update both rows' `field_note_title` to "Auditing Your Mid-Year Contract Amendment Window" and set `field_note_url` to the live Substack URL after Thursday 7:30 AM publish (the checklist already has the "confirm before live" line — the *title* is what's wrong and should be corrected now).
- Pairing rationales reference Channel Pricing as the "structurally similar audit-framework example" — update those one-liners to the amendment Field Note once the title is corrected.

### PART 4 — Substack Notes — **PASS**
- 3/day, origin-heavy mix per the revised Notes strategy; launch teasers on Mon/Wed/Thu; Thursday teaser correctly carries the Oct-1 deadline and the toolkit mention. Origin stories are specific and anchored (the CFO-calendar story, the GER "my broker never pulled this number" story). Brokers framed as allies, PBM as structural adversary. ✓

### PART 4B — X — **PASS (one verify-before-post)**
- Strong adversary/decoder skew, LinkedIn-first rule respected (Tue 5 PM clause driver and Thu 12 PM amplifier both scheduled AFTER their LinkedIn posts; Thursday amendment material kept out of Mon–Wed). Tier-A weekend Cuban-bait present (DIR, copay maximizer, NADAC/MAC, GPO offshore). Origin capped at 1/week (Sun 5 PM). 0–1 hashtags, links in first reply. ✓
- **VERIFY — unsourced hard stat:** "Around 80% of U.S. specialty drug spend now flows through pharmacies the PBMs own" (Wed 5 PM). Stat-shock claims should be defensible per the shocking-fact rule. Confirm the ~80% figure against a citable source before posting, or soften to "the large majority." Same caution on "about 80%" if it recurs.

---

## Cross-week checks

- **Triple-distinct (Mon / Tue / Thu):** Mon = H1 *metrics*; Tue = cost-pattern *clauses*; Thu = amendment-window *clauses*. Tue and Thu are both contract-clause subjects — adjacent, and the file already breaks the *visual* repetition (contract-page carousel vs. whiteboard) deliberately, which mitigates it. Topically it holds at the adjacency ceiling, but it's tight. The sharper problem is **Mon↔Wed** (H1 numbers ↔ "demand 3 numbers") sharing the reporting-numbers subject *and* the literal hook (see Post 86). Net: fix Wednesday's opener and the week reads as distinct enough.
- **No recent/coming topic collision:** Amendment-window and H1-review are not run in the surrounding ~6 weeks; Q3 arc (Renewal Countdown next) is set up cleanly by the Friday tease. ✓
- **Format/process note (not a blocker):** W24 predates the hardwired build system (W26-forward), so it uses `## Publishing Timeline` rather than `## Weekly Run of Show` with a Where column, and the post-copy-in-code-block / Library-Update conventions are partially applied. Grandfathered — but the two missing/incorrect items above (Library number, Library Update section, PART 1C Field Note, Thursday bold) are exactly the kind of drift the new system exists to catch, so fix them on this file regardless.

---

## Highest-risk post of the week

**Post 85 — the Tuesday "Library 03" carousel.** It carries two compounding defects: a **duplicate Library number** that publicly breaks the reference-series the format's entire bookmark/long-tail value rests on, and a **missing required Contract Library Update section** so the carousel's three clauses never reach the evergreen reservoir it's supposed to feed. Both are invisible at the craft level (the slides look right), which is what makes them easy to ship by accident. Renumber to Library 04, reconcile the forward schedule, and add the Library Update section before this goes out.

(Runner-up risk: Post 86 Wednesday, for the verbatim Monday-hook reuse and Mon↔Wed reporting-numbers overlap — a reach/cannibalization risk rather than a structural one.)

## Ship-ready?

**No — one more pass.** Five fixes gate the ship, none requiring a rebuild:
1. Renumber Tuesday carousel **Library 03 → Library 04** everywhere; reconcile vs. the planned W25 "Library 04."
2. Add the **`## Substack Contract Library Update — Library 04 Week`** section and merge the three clauses into `substack_contract_language_library.md`.
3. Fix **PART 1C** `field_note_title`/`field_note_url` on both toolkit rows to the amendment Field Note.
4. Strip **markdown bold** from the Thursday Post 87 paste-ready copy block.
5. Rewrite the **Wednesday** opener so it doesn't reuse Monday's exact hook (keep the 3-numbers payload, lead on reporting-access).

Soft cleanups (non-gating): slide-2 "12–18%" stat, Field Note "100 vs hundreds" consistency, Friday word-count label + optional hashtags, X "~80%" sourcing. Once 1–5 are done, the conversion stack (Mon deep dive + newsletter + Thursday Excellence triad) is genuinely strong and the week ships.

---

*Advisory report only. No week file was edited. To apply fixes, run `/humanize` on a specific block or ask for the targeted rewrites.*

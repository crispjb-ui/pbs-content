# Critique — Week 23: Mid-Year Claims Red Flags

**Run date:** 2026-06-07 (Sunday)
**Target:** `newsletters/week_23_mid_year_claims.md` (next-to-publish week; W23 Monday = 2026-06-08)
**Mode:** Advisory red-team. No files edited.

**Week-determination math:** today 2026-06-07; W25 Mon anchor = 2026-06-22; `current_week = 25 + floor((06-07 − 06-22)/7) = 25 + floor(−15/7) = 25 + (−3) = 22`. W22 Monday (06-01) is already past → use `current_week + 1 = 23`. W23 Monday 2026-06-08 = tomorrow. Correct target.

---

## Verdict up front

**NEEDS ANOTHER PASS before ship — but the failures are housekeeping, not craft.** The four shipping LinkedIn posts and the two Substack pieces are well-conceived and mostly hit the bars. What blocks a clean ship is a set of **stale/dead-weight artifacts and convention misses** that will cause a publishing mistake if the file is handed to a scheduler as-is: a paste-ready first comment with design notes baked in, stale PART 5/6/7 referencing bumped content, a `[REPURPOSED — DO NOT SHIP]` carcass still sitting in PART 3, a wrong date in the title, em-dash sentence separators in copy that ships, an identical hashtag block on the Tue and Thu posts, and a Wix Field-Note pairing pointing at the wrong Field Note. Fix those and it ships strong.

---

## Per-post scoring

### Monday — Substack deep dive: "Five Claims Data Patterns That Signal Trouble Before Renewal" — **PASS**
- Story-driven open ("A client came to us last summer…"), thesis early ("Mid-year is the inflection point"), 5 bold section headers, integrated tool (the red-flag checklist). On-format.
- No fabricated stats: deliberately uses "substantial share," "meaningfully," "four to five months." Good, follows the no-invented-numbers rule.
- Self-Funded Employer Guidance pillar; clean and distinct from Tue/Thu.
- Minor: Sources are generic ("PBMI Annual Reporting Standards," "Self-Insured Group Health Plan Claims Analysis Best Practices"). Not a blocker, but a named, linkable source would harden it.

### Monday — LinkedIn Newsletter (Issue #18) — **PASS**
- Story open mirrors the deep dive; thesis stated as a clear POV ("mid-year is the most valuable window… most employers are not using it"). Executive-briefing register.
- All three required subsections present: **Title Options (3)**, **Publish Post Copy**, **First Comment**. Convention met. Hashtags `#PBMRenewal #SelfFundedEmployers #PharmacyBenefits` — 3, PascalCase, topic-anchor swap on slot 2. Good.
- First comment routes to the gated toolkit landing page with the correct fallback note. Good.

### Tuesday — Post 81: Library 03 Contract Comparison Carousel (Audit Rights) — **PASS (one soft flag)**
- Correct framework (Library NN save-asset), correct metric framing (12+ saves, do not judge on decoder floor), correct first-comment routing to the Contract Language Library. Library-week section present (`## Substack Contract Library Update — Library 03 Week`) with the five §10.01–10.05 clauses listed. Convention met.
- Hook is a knowledge-gap shape ("…decide whether your audit happens on your terms or theirs"). Passes.
- **Soft flag (proprietary anchor):** the post-copy anchor is "The brokers we work with check these five before signing." That's broker-as-ally framing (good) but it's thinner than the Thursday post's "hundreds of contracts a year." Consider adding one PBS-specific detail to the copy itself ("on the contracts we review, audit rights run three to five lines and hand the PBM every parameter") so the anchor lives in the post, not only the slides.
- **Em-dash (brand rule) — FLAG:** line 486 post copy: *"The protective version names every parameter — frequency, scope, who can audit…"* uses an em-dash as a clause separator. Replace with a colon: "…names every parameter: frequency, scope, who can audit…". The numbered label lines (490–494, "Audit frequency and notice period — …") read as label dashes (allowed, like "Slide 1 - Cover"), but the narrative line 486 ships to the feed and breaks the rule.

### Wednesday — Post 82: "A Dermatologist Explained PBMs Better Than Anyone in Pharmacy" (Zirwas analogy) — **PASS (highest-variance post; see risk note)**
- Confrontational/knowledge-gap hook naming a surprising actor (a dermatologist). Passes the Wednesday confrontation test.
- Working framework: full-system decoder (rebate inflation → list price, fail-first → step therapy, prove-it → prior auth, owns-the-buildings → owns-the-pharmacy). In the WORKING set.
- Proprietary anchor present: "I read PBM contracts for a living," "I can point to every one of those mechanisms in the contracts on my desk. Hundreds of them a year." Good.
- Humanize Check: one formula (analogy decode), not stacked; no AI-tell blacklist phrases; broker/agent kept as ally, PBM as adversary. The housing dollars are explicitly hypothetical, so no fabricated-statistic issue. Permission documented (Zirwas, used with permission, credited in body + visual footer). Clean.
- **Watch (not a blocker):** it's the week's longest post by a wide margin and leans entirely on a borrowed analogy, so PBS's proprietary differentiation is thinner than usual and the dwell risk is real. Confirm Zirwas permission is still good and the credit is in the visual footer before scheduling.

### Thursday — Post 83: "5 Admin Fee Terms Decoded" (messy infographic) — **PASS on craft / FLAG on the first-comment block**
- Strongest-built post of the week. 5-trait Thursday Excellence checklist all met and documented: photoreal annotated-invoice format ✓; confrontational named-actor hook ("Your PBM names the fees… writes what each one means… sends you the bill") ✓; proprietary anchor ("hundreds of PBM contracts a year at PBS") ✓; first comment names the same-day Field Note ✓; Field Note ("Decoded the Admin Fees. Now Audit Them.") exclusively supports it ✓. 9/10 winning traits documented.
- Mobile-feed legibility rule explicitly engineered into the prompt (five items, dominate center 70%, thumbnail-legible labels). Good.
- **FLAG (paste-ready hygiene — will ship garbage):** the First Comment fenced code block (lines ~841–852) contains the 2-sentence Field Note reference **plus a "Content outline (5 terms…)" design-spec block** inside the same code fence. If pasted as-is, the design notes go into the LinkedIn comment. Split it: the fenced block should contain ONLY the comment + `[SUBSTACK FIELD NOTE URL]`. Move the content outline out of the code fence into a plain `#### Content outline (design ref)` note.
- **FLAG (hashtags) — identical block to Post 81:** Post 83 uses `#PBMContracts #SelfFundedEmployers #PharmacyBenefits` — the **exact same three tags as the Tuesday Library 03 post**, and both are PBM Contract Insights pillar. CLAUDE.md: "if Tuesday and Thursday both fall on the same pillar, vary the topic slot." Swap the Thursday topic slot, e.g. `#PBMContracts #PBMCompensation #PharmacyBenefits` or `#AdminFees`.

### Friday — Post 84: "Your PBM Already Knows Your H1 Numbers" tease — **PASS (likely-floor caution)**
- Knowledge-gap hook ("They know… You probably do not"). Teases W24 H1 review. Topic hand-off to next week is clean.
- **Caution (performance):** tracker shows prose-only Friday teases without a named adversary land in the 500–1K range; the one Friday tease that broke the floor (W20, 2,220) used an **itemized-list** structure. This tease is prose. Consider reshaping into the itemized "five numbers your PBM already modeled / you haven't seen" list to replicate the W20 floor-break. Not a blocker.

---

## Cross-week & build-spec gate findings

### Triple-distinct (Mon / Tue / Thu+Field Note) — **FLAG (adjacency at the ceiling)**
- Mon: Mid-Year Claims data patterns (Self-Funded / claims). Distinct. ✓
- Tue: Library 03 **Audit Rights** clauses (PBM Contract Insights).
- Thu LinkedIn + Field Note: **5 Admin Fee Terms / "Now Audit Them"** (PBM Contract Insights).
- Tue and Thu are **both PBM Contract Insights and both frame "auditing your PBM contract"** (audit-rights clauses vs. auditing fee line items). This sits right at the adjacency ceiling and arguably collapses to one subject family ("audit your PBM"). Monday is safely distinct, so the cannibalization risk is Tue↔Thu, not Mon↔anything. **Recommendation:** keep them but sharpen the framing so they read as different subjects — Tuesday = "your *right* to audit" (the clause that grants access), Thursday = "what to audit *first*" (the fee line items). The current hooks already lean this way; just make the distinction explicit in the copy so a reader doesn't see two "audit your PBM" posts three days apart.

### Gate 5 — Run of Show — **FAIL**
- File opens with **`## Publishing Timeline`**, not the required **`## Weekly Run of Show`**, and it has **no "Where" column** pointing each row to its PART/Post #. Spec requires the renamed section with the Where column reconciled to built content. Rename + add the Where column.

### Gate 6 — No dead weight in the live file — **FAIL**
- **`[REPURPOSED — DO NOT SHIP TO LINKEDIN]` carcass still present:** the entire "Five Mid-Year Claims Red Flags (Infographic)" block (lines ~717–808) plus its non-shipping first-comment note (741–743) is left in PART 3. Per the deep-dive-visual rule, a visual repurposed into the Monday deep dive must move INTO PART 1 as `### In-Article Visual`, not remain as a dead block in PART 3. Either relocate it to PART 1 or delete it from the live file.
- **Post 82-ALT "BUMPED"** (lines ~639–708, the "Your Claims Data Is Already Telling You" POV + video script) is parked in the live week file. Genuinely-parked drafts belong in `evergreen_visual_backlog.md` / `field_note_backlog.md`, not in the shipping week file. Move it out.

### Stale PART 5 / 6 / 7 (drift) — **FAIL (will mislead the scheduler)**
- **PART 5 Publishing Checklist** still says: "Post 82 - 'Your Claims Data Is Already Telling You' text post" and "Post 83 - Five Mid-Year Claims Red Flags infographic." Both are the **bumped** content. Live Post 82 is the Zirwas dermatologist post; live Post 83 is 5 Admin Fee Terms Decoded.
- **PART 6 Assets Needed** lists "Post 83 Infographic — Five Mid-Year Claims Red Flags" (stale) instead of the admin-fee annotated-invoice asset.
- **PART 7 Metrics** labels "Post 81 (Tue Infographic)" — it's a **carousel**, not an infographic.
- Reconcile PART 5/6/7 to the actual built Posts 81–84.

### PART 1C — Wix Field-Note pairing **mismatch** — **FLAG**
- The Toolkit dataset row sets `field_note_title = "What We See When We Audit Channel Pricing"` and `field_note_url = .../one-drug-class-to-watch-the-next`, and the pairing rationale says "Channel Pricing Field Note covers the most common Red Flag pattern." But **this week's actual Thursday Field Note is "Decoded the Admin Fees. Now Audit Them."** Either (a) point the row's `field_note_*` at this week's admin-fee Field Note once it publishes, or (b) if the Channel Pricing Field Note is intentionally the evergreen pairing for the Mid-Year toolkit, say so explicitly. As written it reads as a copy-paste leftover.

### Gate 2 — Shocking-fact weaving — **VERIFY (likely partial)**
- No clearly-sourced shocking fact from `shocking_fact_bank.md` is woven into a LinkedIn post with citation framing ("the FTC found…"). The closest is the Wed X 5:00 PM "three PBMs process about 80% of U.S. prescription claims" — plausible and widely cited, but it carries no source tag and lives on X, not the LinkedIn breakout. Spec wants the fact in a real post (Thursday breakout first, else Wednesday POV / X AM amplifier) **with its citation**. Confirm this week's mapped bank entry and either weave it (cited) into the Wednesday or Thursday post or log the gate as a reasoned skip. The "80%" X line should get a source anchor regardless.

### Title date error — **FLAG (brand: dates must be correct)**
- File header reads **"## June 9-13, 2026,"** but the week is **Mon Jun 8 – Fri Jun 12** (the body publish lines correctly say "Monday, June 8, 2026"). The title is off by one day. Fix to "June 8-12, 2026" (or 8-14 if including the weekend X run).

### Em-dash sentence separators — **FAIL (brand rule)**
- Clear narrative-prose violations that ship:
  - Field Note line 199: *"…the invoice the PBM sends you each month — and on most contracts I review…"* → use a period or colon.
  - Field Note blockquote items 219, 227, 231 (e.g. *"…operational support — three line items…"*; *"…rather than 'rebate' — keeps them outside…"*; *"Line items only — not the contract."*) → recast with commas/colons/periods.
  - Post 81 copy line 486 (flagged above).
- Sweep the whole file for " — " in any copy that ships (post bodies, Field Note, deep dive) and replace per the brand rule. Label dashes in spec lines ("Slide 1 - Cover," numbered label lists) are fine.

---

## Single highest-risk item of the week

**The Thursday Post 83 First Comment as currently fenced** is the highest-risk shippable defect: the paste-ready code block contains design-spec "Content outline" notes alongside the actual comment, so a publisher copying the block will paste internal notes into a public LinkedIn comment on the week's highest-conversion post (the Thursday cross-promo that drove 4–5 Substack subs in the W18 pattern). It must be split before scheduling.

**Highest-variance post:** the Wednesday Zirwas analogy — the week's biggest reach bet, very long, and dependent on borrowed material; confirm permission/credit and accept the dwell risk knowingly.

---

## Ship-readiness

**Not ship-ready as-is — one more pass.** None of the four LinkedIn posts or two Substack pieces needs a craft rewrite; the content is strong and the Thursday post is excellent. The blockers are mechanical and fast to fix:

1. Split the Post 83 first-comment code block (comment only; outline out of the fence). **(must-fix)**
2. Reconcile PART 5 / 6 / 7 to the live Posts 81–84. **(must-fix)**
3. Remove the `[REPURPOSED — DO NOT SHIP]` red-flags block and the Post 82-ALT from the live file (relocate per the deep-dive-visual / backlog rules). **(must-fix)**
4. Fix the title date (June 8-12). **(must-fix)**
5. Replace em-dash separators in shipping copy (lines 199, 219, 227, 231, 486 + full sweep). **(must-fix)**
6. Vary the Thursday hashtag block off the Tuesday block. **(must-fix)**
7. Fix the PART 1C Wix `field_note_*` pairing (point at the admin-fee Field Note, or justify the evergreen Channel Pricing pairing). **(should-fix)**
8. Rename `Publishing Timeline` → `Weekly Run of Show` + add the Where column. **(should-fix, gate)**
9. Confirm/weave the cited shocking fact and source-tag the X "80%" stat (gate 2). **(should-fix, gate)**
10. Optional craft lifts: add a PBS proprietary anchor to Post 81 copy; reshape Friday tease into the W20 itemized-list structure; explicitly differentiate the Tue audit-rights vs. Thu admin-fee-audit framing.

Items 1–6 are the gate to schedule. 7–9 close build-spec gates. 10 is upside.
